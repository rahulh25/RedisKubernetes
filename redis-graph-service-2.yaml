apiVersion: v1
kind: Service
metadata:
  name: redisgraph-service-2
spec:
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the frontend service.
  type: LoadBalancer
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: redisgraph-deployment-6888bfbbb6-6fjkk